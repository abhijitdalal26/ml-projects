Run `train-model/FashionMNIST-Notebook.ipynb` on Colab, unzip the downloaded
`fashion_mnist_tfjs.zip`, and drop `model.json` + the weight shard(s) directly
into this folder (same layout as `MNIST/model/`).
